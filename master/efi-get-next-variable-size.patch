diff --git a/xen/common/efi/runtime.c b/xen/common/efi/runtime.c
index 7c767e4..b593d39 100644
--- a/xen/common/efi/runtime.c
+++ b/xen/common/efi/runtime.c
@@ -524,7 +524,7 @@ int efi_runtime_call(struct xenpf_efi_runtime_call *op)
 
             if ( !EFI_ERROR(status) &&
                  copy_to_guest(op->u.get_next_variable_name.name,
-                               name.raw, size) )
+                               name.raw, op->u.get_next_variable_name.size) )
                 rc = -EFAULT;
             op->u.get_next_variable_name.size = size;
         }
