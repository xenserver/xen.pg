# HG changeset patch
# Parent 87818c0d1fbfc9c91953abf8bc1296dbefbff473

diff -r 87818c0d1fbf xen/Makefile
--- a/xen/Makefile
+++ b/xen/Makefile
@@ -2,7 +2,8 @@
 # All other places this is stored (eg. compile.h) should be autogenerated.
 export XEN_VERSION       = 4
 export XEN_SUBVERSION    = 3
-export XEN_EXTRAVERSION ?= .0$(XEN_VENDORVERSION)
+export XEN_SUBSUBVER     = .0
+export XEN_EXTRAVERSION ?= $(XEN_SUBSUBVER)$(XEN_VENDORVERSION)
 export XEN_FULLVERSION   = $(XEN_VERSION).$(XEN_SUBVERSION)$(XEN_EXTRAVERSION)
 -include xen-version
 
@@ -111,7 +112,7 @@ delete-unfresh-files:
 
 .banner: Makefile
 	$(MAKE) -C tools
-	@tools/figlet/figlet -d tools/figlet Xen $(XEN_FULLVERSION) 2>$@2 >$@1
+	@tools/figlet/figlet -d tools/figlet Xen $(XEN_VERSION).$(XEN_SUBVERSION)$(XEN_SUBSUBVER) 2>$@2 >$@1
 	@cat $@1 $@2 >$@
 	@rm -f $@1 $@2
 
